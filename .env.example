# StarStreamer Development Environment Variables
# Copy this file to .env for development logging overrides
# All other configuration is in config.yaml

# === DEVELOPMENT LOGGING OVERRIDES ===
# These are the only variables loaded from .env

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Debug mode (more verbose logging and error details)
# DEBUG=false
